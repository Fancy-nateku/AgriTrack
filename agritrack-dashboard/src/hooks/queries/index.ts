// Query hooks are deprecated in favor of direct hooks
// These files are kept for backward compatibility but are not actively used
// The application now uses:
// - useIncome from '@/hooks/useIncome'
// - useExpenses from '@/hooks/useExpenses'
// - useDefaultFarm from '@/hooks/useFarm'

export {};
